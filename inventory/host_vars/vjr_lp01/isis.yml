junos_isis:
  iso:
    interface: lo0.11
    address: "49.0001.0010.0000.0011.00"
  interfaces:
    - name: "{{ junos_lt_interface }}.2"
      config:
        - point-to-point ldp-synchronization # commen ld-syn for rsvp
    - name: "{{ junos_lt_interface }}.3"
      config:
        - point-to-point ldp-synchronization # commen ld-syn for rsvp
    - name: "{{ junos_lt_interface }}.5"
      config:
        - point-to-point ldp-synchronization # commen ld-syn for rsvp
    - name: lo0.11
      config:
        - "passive"

